etag:
    Type: TypeString
    Computed: "true"
org_id:
    Type: TypeString
    Description: The numeric ID of the organization in which you want to manage the audit logging config.
    Required: "true"
policy_data:
    Type: TypeString
    Computed: "true"

